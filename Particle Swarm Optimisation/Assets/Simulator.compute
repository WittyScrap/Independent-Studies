#pragma kernel CSMain

RWTexture2D<float4> Result;

// Target properties...
int OutputWidth;
int OutputHeight;

// Main kernel
[numthreads(8,8,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    Result[id.xy] = float4(id.x / (float)OutputWidth, id.y / (float)OutputHeight, 0, 0);
}
